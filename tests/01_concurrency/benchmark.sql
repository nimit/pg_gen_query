SELECT * FROM pg_gen_query('Show me all orders shipped from the East region with the customers name and city.');
SELECT * FROM pg_gen_query('List products that are low in stock (below 20 units) in any warehouse.');
SELECT * FROM pg_gen_query('Which customers ordered electronics but have never ordered clothing?');
SELECT * FROM pg_gen_query('What were the total quantities shipped per region last month?');